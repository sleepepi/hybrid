$("#query_concepts_popup").modal({ dynamic: true });
$("#query_concepts_popup").modal('hide');

$("#query_concepts").html("<%= escape_javascript(render("query_concepts/query_concepts")) %>");

img_undo = $('#undo_img');
img_redo = $('#redo_img');

img_undo.attr('data-img-state', '<%= @query.undo? ? "enabled" : "disabled" %>');
img_redo.attr('data-img-state', '<%= @query.redo? ? "enabled" : "disabled" %>');

img_undo.attr('src', root_url + 'assets/' + img_undo.attr('data-img-'+img_undo.attr('data-img-state')));
img_redo.attr('src', root_url + 'assets/' + img_redo.attr('data-img-'+img_redo.attr('data-img-state')));



$('#selected_concept_id').val('');
$('#concept_search_term').val('');
$( "#query_form" ).submit();
$( "#report_table_form" ).submit();
