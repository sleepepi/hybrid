<%= form_for(@query_user) do |f| %>
  <% if @query_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query_user.errors.count, "error") %> prohibited this query_user from being saved:</h2>

      <ul>
      <% @query_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
