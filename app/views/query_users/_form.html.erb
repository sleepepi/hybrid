<%= form_for(@query_user) do |f| %>
  <% if @query_user.errors.any? %>
    <div id="error_explanation" class="bs-callout bs-callout-danger">
      <h4><%= pluralize(@query_user.errors.count, "error") %> prohibited this query_user from being saved</h4>

      <ul>
      <% @query_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
