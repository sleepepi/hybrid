hideContourModal();

$("#criteria").html("<%= escape_javascript(render("criteria/criteria")) %>");

img_undo = $('#undo_img');
img_redo = $('#redo_img');

img_undo.attr('data-img-state', '<%= @search.undo? ? "enabled" : "disabled" %>');
img_redo.attr('data-img-state', '<%= @search.redo? ? "enabled" : "disabled" %>');

img_undo.attr('src', img_undo.attr('data-img-'+img_undo.attr('data-img-state')));
img_redo.attr('src', img_redo.attr('data-img-'+img_redo.attr('data-img-state')));

$("#search_form").submit();
$("#report_table_form").submit();
